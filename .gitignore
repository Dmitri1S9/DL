# Models (too large for GitHub, downloaded automatically)
# Anchored to repo root so it doesn't also match the src/model package.
/models/

# Virtual environment
.venv/

# Generated audio (not needed in the repo)
/audio/

# Logs (created at runtime)
/logs/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Data (downloaded via src/data/download.py)
# Anchored to repo root so it doesn't also match the src/data package.
/data/

# IDE
.vscode/
.idea/

# Stray checkpoints in repo root (real ones live in /models/, already ignored)
*.pt

# macOS / tooling cruft
.DS_Store
.pytest_cache/
.ruff_cache/
