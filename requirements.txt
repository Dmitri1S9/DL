# Exact, pinned versions — verified working together end-to-end (real synthesis,
# Whisper eval, and the LJSpeech download). Re-pin from a known-good venv with:
#     .venv/bin/pip freeze
torch==2.12.0
torchaudio==2.11.0
transformers==5.10.1
datasets==3.6.0  # <4: 4.x removed dataset-loading scripts; keithito/lj_speech needs them
soundfile==0.13.1
librosa==0.11.0
numpy==2.4.6
scipy==1.17.1
jiwer==4.0.0
accelerate==1.13.0
sentencepiece==0.2.1
loguru==0.7.3
pydantic==2.13.4
openai-whisper==20250625  # ASR for the WER/CER intelligibility metric
pymcd==0.2.1  # canonical MCD (DTW + WORLD mel-cepstral) for the spectral metric
